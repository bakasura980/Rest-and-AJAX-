export class Car {
    constructor(private model: string, private description: string, private image_path: string,
        private engine: string, private transmission: string, private state: string, private power: string, private color: string) { }
}